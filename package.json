{
  "name": "vitevuewithwasm",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "yarn prod_build",
    "dev_build": "yarn run wasm_build && vite build --mode development",
    "prod_build": "yarn run wasm_build && vite build --mode production",
    "wasm_build": "node wasm.compile.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.2.37"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.1.0",
    "rollup-obfuscator": "^3.0.1",
    "vite": "^3.1.0"
  }
}
